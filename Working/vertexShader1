#version 460

layout(location = 0) in vec3 vertPos;
layout(location = 1) in vec3 vertColour;

uniform mat4 mvpMat;
uniform float aspectRatio;

out vec3 colour;

void main()
{
	colour = vertColour;
	vec4 transformedPos = vec4(vertPos, 1.0) * mvpMat;
	gl_Position = vec4(transformedPos);
}